# 方寸观察者模拟学习系统 — Web 前端开发方案（定稿）

> **技术路线**：Python `ThreadingHTTPServer` + 原生 HTML/CSS/JS + SSE | **目标平台**：Windows 本地 | **依赖**：零新增 | **版本**：v1.0-web-final
>
> **定稿说明**：本文档基于《前端开发方案初稿》经全面评审后产出，已修正初稿中 8 项关键问题并补充 3 项缺失功能。所有技术决策已与需求方对齐。

---

## 第一部分：总览

### 1.1 一句话定义

在现有 CLI 演示系统（`demo.py`）基础上新增 Web 模式（`app.py`），使用 Python 标准库 `http.server.ThreadingHTTPServer` 提供 HTTP 服务 + SSE 实时推送，前端为单个 `index.html` 文件（HTML/CSS/JS 内联，零框架），核心业务逻辑（`observer_core/`）零改动，CLI 与 Web 并存。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| **零依赖** | 不新增任何 `pip install` 或 npm 包，全部使用 Python 标准库 + 浏览器原生 API |
| **全白盒** | ~1100 行新增代码（`app.py` ~450 行 + `index.html` ~550 行 + `analysis_panels.py` ~100 行），全部可读可调试 |
| **不改核心** | `observer_core/`、`scenarios/`、`rules/`、`models/` 零改动 |
| **CLI 共存** | `python demo.py` 和 `python app.py` 各自独立运行，互不干扰 |
| **单文件前端** | 一个 `index.html`（~550 行），浏览器直接打开源码即文档 |
| **线程安全** | `ThreadingHTTPServer` 每请求独立线程，SSE 长连接不阻塞其他 HTTP 请求 |

### 1.3 前后端职责边界

```
┌─────────────────────────────────────────────────────────┐
│                     浏览器 (前端)                        │
│                                                         │
│  职责:                                                   │
│  · 渲染 UI (导航/菜单/日志/面板)                          │
│  · 处理用户交互 (点击按钮 -> 发送 HTTP 请求)              │
│  · 消费 SSE 事件流 (逐步展示场景运行过程)                 │
│  · 格式化展示 JSON/Markdown 内容                         │
│                                                         │
│  技术: 纯 HTML + 内联 CSS + 内联 JS                      │
│        fetch() + EventSource + DOM API                  │
└─────────────────────────┬───────────────────────────────┘
                          |  HTTP/1.1
                          |  GET/POST + SSE
                          v
┌─────────────────────────────────────────────────────────┐
│          Python ThreadingHTTPServer (app.py)             │
│                                                         │
│  职责:                                                   │
│  · HTTP 路由分发 (每请求独立线程)                         │
│  · JSON API (场景列表/分类/报告/删除)                     │
│  · SSE 端点 (场景运行逐步推送)                            │
│  · 线程管理 (后台执行场景，请求线程服务 HTTP)             │
│  · 静态文件服务 (index.html)                             │
│  · 路径安全校验 (防止路径遍历攻击)                        │
│                                                         │
│  技术: ThreadingHTTPServer + json + threading + queue    │
│        + pathlib + urllib.parse + uuid + subprocess      │
│        全部标准库，零 pip install                         │
└─────────────────────────┬───────────────────────────────┘
                          | 直接 import
                          v
┌─────────────────────────────────────────────────────────┐
│          现有核心逻辑 (observer_core/ 等)                 │
│                                                         │
│  零改动。与 demo.py 调用的是完全相同的模块和函数           │
└─────────────────────────────────────────────────────────┘
```

### 1.4 评审修正摘要

| 编号 | 初稿问题 | 定稿修正 |
|:---:|---------|---------|
| P0-1 | `HTTPServer` 单线程，SSE 阻塞其他请求 | 改用 `ThreadingHTTPServer`，每请求独立线程 |
| P0-2 | `run_scenario_stream()` 封装方式模糊 | 新增 `StreamScenarioRunner` 适配层（~80行），明确字段定义 |
| P0-3 | 报告查看接口存在路径遍历风险 | 强制 `os.path.realpath` 校验，限制在 `output/reports/` 内 |
| P1-1 | 全量运行 SSE 无崩溃恢复 | 保留单 SSE + 心跳 15s + 进度查询接口 |
| P1-2 | 单元测试 SSE 实现过度复杂 | 简化为同步模式，`POST /api/tests/run` 一次性返回结果 |
| P1-3 | SSE 重连幂等性未处理 | 重连前先查状态，不回放事件 |
| P1-4 | 前端视图状态机不完整 | SSE 与视图解耦，日志 buffer 恢复，停止按钮 |
| P1-5 | 服务器关闭时未处理活跃 SSE | 遍历 `active_runs` 发送 error 事件 + 线程 shutdown |
| P2-1 | 心跳间隔不一致（15s vs 30s） | 统一为 15 秒 |
| P2-2 | "浏览器默认超时 30-120s" 描述不准确 | 删除该说明，EventSource 无硬性超时 |
| P2-3 | `index.html` ~400 行偏紧 | 放宽到 ~550 行 |
| P2-4 | `app.py` ~300 行估算不足 | 放宽到 ~450 行 |
| F1 | SA 字典仅 `demo.py` 有 | 提取为 `analysis_panels.py`，两端共享 |
| F2 | 场景元信息未定义来源 | API 从 YAML 动态计算，可选补充 `expected` 字段 |
| F3 | 报告查看无 Markdown 渲染 | 内联 ~50 行 JS 精简解析器 |

---

## 第二部分：系统架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                       浏览器 (localhost:8080)                      │
│                                                                    │
│  static/index.html (单文件, ~550行)                                │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  HTML: #sidebar (导航) + #main-content (视图动态切换)        │ │
│  │  CSS:  终端风格深色主题, 等宽字体日志, 响应式布局             │ │
│  │  JS:   Router + API + SSE + UI Renderer + Markdown渲染       │ │
│  └──────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬──────────────────────────────────────┘
                            │  HTTP + SSE
                            v
┌──────────────────────────────────────────────────────────────────┐
│           Python ThreadingHTTPServer (app.py, ~450行)              │
│                                                                    │
│  路由表 (14 个端点):                                               │
│    GET  /                    → index.html                          │
│    GET  /api/health          → 健康检查                            │
│    GET  /api/categories      → 场景分类                            │
│    GET  /api/scenarios       → 场景列表                            │
│    POST /api/scenario/run    → 启动场景 (返回 run_id)              │
│    GET  /api/scenario/run-all → SSE 批量运行                       │
│    GET  /api/scenario/run-all/progress → 全量进度                  │
│    GET  /api/scenario/stream/<id> → SSE 事件流                    │
│    GET  /api/scenario/result/<id> → 分析面板 JSON                 │
│    POST /api/tests/run       → 同步运行单元测试                    │
│    POST /api/reports/delete  → 删除记录                            │
│    GET  /api/reports/list    → 报告列表                            │
│    GET  /api/reports/view    → 报告内容 (含路径校验)               │
│    POST /api/server/stop     → 关闭服务器                          │
│                                                                    │
│  线程模型:                                                         │
│    ThreadingHTTPServer: 每请求独立线程                              │
│    工作线程: StreamScenarioRunner -> Queue -> SSE 推送              │
│    active_runs: dict[str, Queue]  (线程安全)                       │
│    run_results: dict[str, dict]                                    │
└────────────────────────────────────────────────────────────────────┘
                                    │ import
                                    v
┌──────────────────────────────────────────────────────────────────┐
│            现有核心逻辑 (零改动)                                   │
│  observer_core/  scenarios/  rules/  models/  evolution/          │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 核心数据流

```
用户点击 "运行场景 A01"
  |
  v
前端: fetch POST /api/scenario/run {scenario_id: "a01"}
  |
  v
后端: 生成 run_id, 创建 Queue, 启动后台线程, 返回 {run_id: "xxx"}
  |
  v
前端: 收到 run_id, 先查 result 接口确认状态, 然后创建 EventSource
  |
  v
后端: GET /api/scenario/stream/xxx -> SSE 循环, 阻塞读 Queue
  |   (ThreadingHTTPServer 中此阻塞不影响其他请求)
  v
后台线程: StreamScenarioRunner.run_stream() 生成器
           for each event: 归一化 -> 规则匹配 -> 评分 -> 研判 -> 处置
           yield step_data -> put 到 Queue
  |
  v
SSE端点: Queue get -> wfile.write("data: {...}\n\n") -> flush
  |      (每 15 秒无事件发送 heartbeat)
  v
前端: EventSource.onmessage -> JSON.parse -> 追加日志 + logBuffer
  |
  v
后台线程: 全部完成 -> yield {type: "done", analysis_panel: {...}}
  |
  v
SSE端点: write done -> break -> 关闭连接
  |
  v
前端: type==="done" -> 渲染分析面板 -> es.close()
```

### 2.3 线程模型（定稿版）

```
ThreadingHTTPServer 线程池:
  每个 HTTP 请求由独立线程处理
  普通 GET/POST: 处理完立即返回
  SSE 端点: 在 do_GET 内阻塞读取 Queue (仅影响该请求线程)

工作线程 (daemon=True, 每个场景一个):
  def _run_scenario_async(scenario_id, event_queue):
      runner = StreamScenarioRunner(...)
      for step_data in runner.run_stream():  # 生成器
          event_queue.put(step_data)

关键数据结构 (线程安全):
  active_runs: dict[str, Queue]   # run_id -> 事件队列
  run_results: dict[str, dict]    # run_id -> 分析面板数据
  run_status:  dict[str, str]     # run_id -> "running"/"done"

注: dict 单操作在 CPython 中原子, Queue 线程安全, 当前无需 Lock
```

---

## 第三部分：API 接口定义

### 3.1 接口总览（14 个端点）

| 方法 | 路径 | 作用 | 类型 |
|------|------|------|------|
| GET | `/` | 返回 `index.html` | 静态 |
| GET | `/api/health` | 健康检查 | JSON |
| GET | `/api/categories` | 场景分类列表 | JSON |
| GET | `/api/scenarios?category={name}` | 分类下场景列表 | JSON |
| POST | `/api/scenario/run` | 启动单个场景 | JSON |
| GET | `/api/scenario/run-all?category={name}` | SSE 批量运行 | SSE |
| GET | `/api/scenario/run-all/progress` | 全量运行进度 | JSON |
| GET | `/api/scenario/stream/{run_id}` | SSE 事件流 | SSE |
| GET | `/api/scenario/result/{run_id}` | 分析面板 JSON | JSON |
| POST | `/api/tests/run` | 运行单元测试（同步） | JSON |
| POST | `/api/reports/delete` | 删除测试记录 | JSON |
| GET | `/api/reports/list` | 报告文件列表 | JSON |
| GET | `/api/reports/view?path=...` | 报告内容 | text |
| POST | `/api/server/stop` | 关闭服务器 | JSON |

### 3.2 接口详细定义

#### GET `/api/categories`

```json
{
  "categories": [
    {"id": "normal", "name": "Normal 正常行为", "count": 8, "description": "..."},
    {"id": "anomalous", "name": "Anomalous 异常行为", "count": 12, "description": "..."}
  ]
}
```

#### GET `/api/scenarios?category=anomalous`

```json
{
  "category": "anomalous",
  "scenarios": [
    {
      "id": "a01-rm-root",
      "name": "A01-高危系统命令:递归删除根目录",
      "event_count": 3,
      "expected_blocks": 1,
      "expected_alerts": 0,
      "description": "Agent执行rm -rf / --no-preserve-root"
    }
  ]
}
```

实现：读取 YAML，从 `event_sequence` 计算 `event_count`，从 `expected` 字段推断。若无 `expected` 字段返回 `null`。

#### POST `/api/scenario/run`

```json
// 请求: {"scenario_id": "a01", "category": "anomalous"}
// 返回: {"run_id": "a01_20260728_130332", "scenario_id": "a01", "status": "running"}
```

实现：生成 run_id -> 创建 Queue -> 启动 daemon 线程 -> 立即返回 run_id。

#### GET `/api/scenario/stream/{run_id}`

SSE 端点。Content-Type: text/event-stream。每 15 秒心跳。收到 done/error 时关闭。

#### GET `/api/scenario/result/{run_id}`

```json
{
  "run_id": "a01_20260728_130332",
  "scenario_id": "a01",
  "status": "completed",
  "duration_ms": 1234,
  "statistics": {"total_events": 3, "allow": 2, "alert": 0, "block": 1},
  "analysis_panel": {
    "test_purpose": "检测并阻断rm -rf /等高危破坏性命令",
    "risk_findings": "命中R001(block-dangerous-commands)",
    "root_cause": "命令模式直接匹配R001最高优先级规则",
    "comprehensive_strategy": "Tier2阻断当前exec事件"
  },
  "files_generated": [
    {"type": "md", "path": "output/reports/..."},
    {"type": "json", "path": "..."}
  ]
}
```

#### GET `/api/scenario/run-all?category=anomalous`

SSE 批量运行。事件类型：scenario_start / step / scenario_done / all_done / heartbeat。单个场景失败不中断后续。

#### GET `/api/scenario/run-all/progress`

```json
{"completed": 12, "total": 37, "current_scenario": "a05", "status": "running"}
```

#### POST `/api/tests/run`（同步模式）

```json
// 同步等待 pytest 完成 (约 10-30 秒), 前端显示 loading spinner
{
  "status": "completed", "total": 130, "passed": 130,
  "failed": 0, "failed_names": [], "duration_seconds": 12.5
}
```

#### POST `/api/reports/delete`

```json
// 请求: {"scope": "all"} | {"scope": "category", "category": "normal"} | {"scope": "scenario", "scenario_id": "n01"}
// 返回: {"success": true, "deleted": {"directories": 5, "files": 12}}
```

#### GET `/api/reports/list`

```json
{"categories": {"normal": [{"scenario_id": "n01", "timestamps": ["20260728_130332"], "count": 1}]}}
```

#### GET `/api/reports/view?path=...`

安全约束：`os.path.realpath()` + 前缀校验，不在 REPORTS_DIR 下返回 403。

```python
REPORTS_DIR = os.path.realpath(os.path.join(BASE_DIR, "output", "reports"))

def _serve_report_file(self, file_path):
    requested = os.path.realpath(os.path.join(self.REPORTS_DIR, file_path))
    if not requested.startswith(self.REPORTS_DIR + os.sep) and requested != self.REPORTS_DIR:
        self._send_error(403, "Access denied")
        return
    if not os.path.isfile(requested):
        self._send_error(404, "File not found")
        return
    with open(requested, "r", encoding="utf-8") as f:
        content = f.read()
    self._send_text(content, content_type="text/plain; charset=utf-8")
```

#### POST `/api/server/stop`

```python
def _handle_server_stop(self):
    for run_id, q in list(active_runs.items()):
        try:
            q.put_nowait({"type": "error", "message": "Server shutting down", "code": "server_shutdown"})
        except Exception:
            pass
    time.sleep(0.5)
    def _shutdown():
        self.server.shutdown()
    threading.Thread(target=_shutdown, daemon=True).start()
    self._send_json({"success": True, "message": "Server shutting down"})
```

### 3.3 SSE 事件格式规范

| 事件类型 | event 字段 | 触发时机 |
|---------|:---:|------|
| 场景步骤 | `step` | 每个事件处理完毕 |
| 场景完成 | `done` | 场景全部事件处理完 |
| 批量-场景开始 | `scenario_start` | run-all 每个场景开始 |
| 批量-场景完成 | `scenario_done` | run-all 每个场景完成 |
| 批量-全部完成 | `all_done` | 37 场景全部完成 |
| 心跳 | `heartbeat` | 每 15 秒 |
| 错误 | `error` | 异常/服务器关闭 |

### 3.4 `step_data` 字段定义

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | `"step"` | 固定值 |
| `seq` | int | 当前步骤序号（1-based） |
| `total` | int | 总步骤数 |
| `event_type` | string | exec/file_open/net_conn 等 |
| `raw_input` | string | 原始事件描述 |
| `normalized_summary` | string | 归一化后摘要 |
| `rule_match.hit` | bool | 是否命中规则 |
| `rule_match.rules` | array | 命中规则 [{id, name, action}] |
| `risk_score` | float | 0.0~1.0 |
| `risk_level` | string | LOW/MEDIUM/HIGH/CRITICAL |
| `risk_breakdown` | object | 四维评分 {rule, baseline, context, sequence} |
| `decision_action` | string | ALLOW/ALERT/BLOCK |
| `decision_tier` | string | Tier1/Tier2/Tier3 |
| `disposal_summary` | string | 处置结果摘要 |
| `timestamp_ns` | int | 虚拟时钟纳秒 |

---

## 第四部分：前端设计

### 4.1 页面结构

```
index.html (~550行)

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>方寸观察者模拟学习系统</title>
  <style> (内联 CSS, ~150行) </style>

<body>
  #app-container (flex 布局)
  |
  ├── #sidebar (左侧导航, 240px, 固定)
  │   ├── .logo-area (图标 + 系统名称)
  │   ├── <nav>
  │   │   ├── [data-view="tests"]     "运行单元测试"
  │   │   ├── [data-view="scenarios"] "按分类浏览场景"
  │   │   ├── [data-view="run-all"]   "全量模拟测试"
  │   │   ├── [data-view="reports"]   "查看报告"
  │   │   └── [data-view="delete"]    "删除测试记录"
  │   └── .server-status (指示灯 + 状态)
  |
  └── #main-content (右侧, flex: 1)
      ├── #view-tests        (按钮 + spinner + 结果面板)
      ├── #view-scenarios    (5 分类卡片 + 场景列表)
      ├── #view-run-all      (进度条 + 日志 + 统计)
      ├── #view-reports      (文件树 + 内容展示)
      ├── #view-delete       (三级删除控件)
      ├── #view-scenario-run (场景头 + 工具栏 + 日志 + 分析面板)
      └── #notification-area (右上角通知)
```

### 4.2 视图状态机（定稿版）

```javascript
const state = {
    currentView: "scenarios",     // 当前视图
    sseConnection: null,          // 全局唯一 SSE 连接
    sseActive: false,             // SSE 是否在接收数据
    logBuffer: [],                // 场景运行日志缓存
    analysisPanel: null,          // 完成后缓存面板数据
    progressData: null,           // 全量运行进度
    previousView: "scenarios",    // 来源视图 (返回用)
};

// 关键规则:
// 1. SSE 连接与视图解耦 — 视图切换不关闭 SSE
// 2. 日志追加到 logBuffer — 视图重新显示时从 buffer 恢复 DOM
// 3. "停止"按钮 -> sseConnection.close() + 清空状态
// 4. 浏览器关闭 Tab -> beforeunload -> 关闭 SSE

function showView(viewId) {
    state.currentView = viewId;
    document.querySelectorAll(".view").forEach(v => v.style.display = "none");
    document.getElementById(`view-${viewId}`).style.display = "block";
    if (viewId === "scenario-run" && state.logBuffer.length > 0) {
        restoreLogFromBuffer();
    }
    document.querySelectorAll(".nav-item").forEach(n => n.classList.remove("active"));
    document.querySelector(`[data-view="${viewId}"]`)?.classList.add("active");
}

function stopCurrentRun() {
    if (state.sseConnection) {
        state.sseConnection.close();
        state.sseConnection = null;
        state.sseActive = false;
        showNotification("场景运行已停止");
    }
}

function restoreLogFromBuffer() {
    const container = document.getElementById("event-log");
    container.innerHTML = "";
    state.logBuffer.forEach(entry => container.appendChild(entry.cloneNode(true)));
    autoScroll(container);
}
```

### 4.3 SSE 重连逻辑（定稿版）

```javascript
async function connectScenarioStream(runId) {
    // 1. 先检查是否已完成 (重连幂等性)
    try {
        const result = await apiGet(`/api/scenario/result/${runId}`);
        if (result.status === "completed") {
            renderAnalysisPanel(result.analysis_panel);
            renderFileLinks(result.files_generated);
            return;
        }
    } catch (e) {
        showError(`运行记录不存在: ${runId}`);
        return;
    }

    // 2. 正在运行 -> 建立 SSE
    const es = new EventSource(`/api/scenario/stream/${runId}`);
    state.sseConnection = es;
    state.sseActive = true;

    es.onmessage = (e) => {
        const data = JSON.parse(e.data);
        if (data.type === "step") {
            const entry = renderLogEntry(data);
            state.logBuffer.push(entry);
            autoScroll();
        } else if (data.type === "done") {
            renderAnalysisPanel(data.analysis_panel);
            es.close();
            state.sseConnection = null;
            state.sseActive = false;
        } else if (data.type === "error") {
            showError(data.message);
            es.close();
            state.sseConnection = null;
            state.sseActive = false;
        }
    };

    es.onerror = () => {
        es.close();
        state.sseConnection = null;
        setTimeout(() => connectScenarioStream(runId), 1000);
    };
}
```

### 4.4 CSS 设计要点

```
配色方案 (终端风格):
  --bg-primary:    #1a1a2e    深蓝黑背景
  --bg-secondary:  #16213e    稍浅背景
  --bg-card:       #0f3460    卡片背景
  --text-primary:  #e0e0e0    主文字
  --accent-green:  #00ff88    放行/成功
  --accent-yellow: #ffcc00    告警
  --accent-red:    #ff4444    阻断/错误
  --accent-blue:   #4499ff    信息/链接

布局:
  全屏 flex, 侧边栏固定 240px, 主区域 flex:1

事件日志:
  等宽字体 Consolas, 彩色标记, 自动滚动

分析面板:
  卡片样式, 桌面 2 列网格

响应式:
  @media (max-width: 768px) 侧边栏隐藏或汉堡菜单
```

### 4.5 JavaScript 模块设计

```
JS 全局结构 (~400行):

1. 状态管理 (~20行)     - state object
2. API 模块 (~40行)     - apiGet/apiPost + 错误处理
3. SSE 模块 (~60行)     - connectScenarioStream/connectRunAllStream/stop
4. 视图渲染 (~180行)    - showView/renderCategories/renderLogEntry/renderAnalysisPanel
5. 事件绑定 (~60行)     - DOMContentLoaded/按钮click/beforeunload
6. 工具函数 (~40行)     - renderMarkdown/formatTimestamp/decisionIcon/riskColor
```

### 4.6 前端 Markdown 渲染器

```javascript
// 内联轻量 Markdown 解析器 (~50行), 零依赖
function renderMarkdown(md) {
    if (!md) return "";
    let html = md
        .replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;")
        .replace(/^### (.+)$/gm, '<h3>$1</h3>')
        .replace(/^## (.+)$/gm, '<h2>$1</h2>')
        .replace(/^# (.+)$/gm, '<h1>$1</h1>')
        .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
        .replace(/\*(.+?)\*/g, '<em>$1</em>')
        .replace(/`(.+?)`/g, '<code>$1</code>')
        .replace(/^\- (.+)$/gm, '<li>$1</li>')
        .replace(/^---$/gm, '<hr>')
        .replace(/\n/g, '<br>');
    html = html.replace(/(<li>.*?<\/li>(<br>)?)+/g,
        m => `<ul>${m.replace(/<br>/g, "")}</ul>`);
    return html;
}
```

### 4.7 前端关键交互流程

```javascript
// 运行单个场景
async function runScenario(scenarioId, category) {
    state.previousView = state.currentView;
    showView("scenario-run");
    state.logBuffer = [];
    document.getElementById("event-log").innerHTML = "";
    document.getElementById("analysis-panel").innerHTML = "";
    const { run_id } = await apiPost("/api/scenario/run",
        { scenario_id: scenarioId, category: category });
    connectScenarioStream(run_id);
}

// 全量运行
async function runAllScenarios(category) {
    showView("run-all");
    const catParam = category ? `?category=${category}` : "";
    const es = new EventSource(`/api/scenario/run-all${catParam}`);
    state.sseConnection = es;
    es.onmessage = (e) => {
        const data = JSON.parse(e.data);
        if (data.type === "scenario_start") { /* 更新进度 */ }
        else if (data.type === "step") { /* 追加日志 */ }
        else if (data.type === "all_done") { /* 渲染统计, es.close() */ }
    };
    es.onerror = () => {
        es.close();
        setTimeout(() => recoverRunAllProgress(), 1000);
    };
}

// 单元测试 (同步等待)
async function runUnitTests() {
    showView("tests");
    showSpinner("test-spinner");
    const result = await apiPost("/api/tests/run", {});
    hideSpinner("test-spinner");
    renderTestResultPanel(result);
}
```

---

## 第五部分：后端实现要点

### 5.1 关键设计决策

| 决策点 | 方案 | 理由 |
|--------|------|------|
| HTTP 服务器 | `ThreadingHTTPServer` | 每请求独立线程 |
| 路由分发 | `if-elif` + `urllib.parse` | 路由数 <15 |
| SSE 实现 | `wfile.write()` + `flush()` | 原生支持 |
| 线程模型 | `Thread(daemon=True)` + `Queue()` | 标准库线程安全 |
| 路径安全 | `os.path.realpath()` + 前缀校验 | 防路径遍历 |

### 5.2 `do_GET` 路由分发

```python
def do_GET(self):
    parsed = urllib.parse.urlparse(self.path)
    path = parsed.path
    params = urllib.parse.parse_qs(parsed.query)

    if path == "/":
        self._serve_static_file("static/index.html", "text/html")
    elif path == "/api/health":
        self._send_json({"status": "ok"})
    elif path == "/api/categories":
        self._send_json(self._get_categories())
    elif path == "/api/scenarios":
        category = params.get("category", [None])[0]
        self._send_json(self._get_scenarios(category))
    elif path.startswith("/api/scenario/stream/"):
        run_id = path.split("/")[-1]
        self._handle_sse_stream(run_id)
    elif path == "/api/scenario/run-all":
        category = params.get("category", [None])[0]
        self._handle_sse_run_all(category)
    elif path == "/api/scenario/run-all/progress":
        self._send_json(self._get_run_all_progress())
    elif path.startswith("/api/scenario/result/"):
        run_id = path.split("/")[-1]
        self._send_json(self._get_result(run_id))
    elif path == "/api/reports/list":
        self._send_json(self._get_report_list())
    elif path == "/api/reports/view":
        file_path = params.get("path", [None])[0]
        self._serve_report_file(file_path)
    else:
        self._send_error(404, "Not Found")
```

### 5.3 SSE 端点实现

```python
def _handle_sse_stream(self, run_id):
    event_queue = active_runs.get(run_id)
    if not event_queue:
        result = run_results.get(run_id)
        if result:
            self._send_json(result)
            return
        self._send_error(404, f"Run ID not found: {run_id}")
        return

    self.send_response(200)
    self.send_header("Content-Type", "text/event-stream")
    self.send_header("Cache-Control", "no-cache")
    self.send_header("Connection", "keep-alive")
    self.send_header("Access-Control-Allow-Origin", "*")
    self.end_headers()

    try:
        while True:
            try:
                data = event_queue.get(timeout=15)
            except queue.Empty:
                hb = json.dumps({"type": "heartbeat",
                    "timestamp": datetime.now().isoformat()})
                self.wfile.write(
                    f"event: heartbeat\ndata: {hb}\n\n".encode("utf-8"))
                self.wfile.flush()
                continue

            event_type = data.get("type", "step")
            json_data = json.dumps(data, ensure_ascii=False, default=str)
            self.wfile.write(
                f"event: {event_type}\ndata: {json_data}\n\n".encode("utf-8"))
            self.wfile.flush()

            if event_type in ("done", "error"):
                break

    except (BrokenPipeError, ConnectionResetError, OSError):
        pass
    finally:
        if run_id in active_runs:
            del active_runs[run_id]
```

### 5.4 `StreamScenarioRunner` 适配层（定稿核心）

```python
class StreamScenarioRunner:
    """
    app.py 专用适配层。
    复用 ScenarioRunner 的组件初始化逻辑，但将命令式流程转为生成器。
    不修改 ScenarioRunner 和 observer_core/ 的任何代码。
    """

    def __init__(self, base_dir, output_dir, category, scenario_id):
        self.base_dir = base_dir
        self.output_dir = output_dir
        self.clock = VirtualClock(start_ns=1718092800000000000)
        self.normalizer = EventNormalizer(clock=self.clock, window_size=10)
        self.engine = RuleEngine()
        self.engine.load_rules(os.path.join(base_dir, 'rules', 'default_policy.yaml'))
        self.baseline = BaselineChecker(min_warm_events=5)
        self.scorer = RiskScorer()
        self.scorer.register_default_dimensions()
        self.decision_engine = DecisionEngine()
        self.cmd_sender = MockCommandSender()
        self.cmd_sender.connect('mock_pipe')
        self.blocking_coord = BlockingCoordinator(
            clock=self.clock, sender=self.cmd_sender, output_dir=output_dir)
        self.behavior_graph = BehaviorGraph()
        self.audit_logger = AuditLogger(output_dir=output_dir)
        self.report_exporter = ReportExporter(output_dir=output_dir)
        self.run_mgr = RunOutputManager(output_dir, category, scenario_id)
        self.audit_logger.set_output_dir(self.run_mgr.audit_dir)
        self.report_exporter.set_output_dir(self.run_mgr.report_dir)
        self.blocking_coord.set_output_dir(self.run_mgr.evidence_dir)
        self.behavior_graph.reset()
        self.cmd_sender.clear()
        self.stats = {
            'total': 0, 'allow': 0, 'alert': 0, 'block': 0,
            'max_score': 0.0, 'matched_rules': [], 'escalation_count': 0,
        }

    def run_stream(self, scenario_path):
        """生成器：每处理一个事件 yield step_data 字典"""
        with open(scenario_path, 'r', encoding='utf-8') as f:
            data = yaml.safe_load(f)
        scenario = data['scenario']
        self.audit_logger.start_scenario(scenario['id'])
        events = scenario['event_sequence']
        total = len(events)

        for i, event_data in enumerate(events, 1):
            yield self._process_one_event(event_data, scenario, i, total)

        self.audit_logger.close()
        files = self._generate_reports(scenario)
        panel = self._build_analysis_panel(scenario)

        yield {
            "type": "done",
            "analysis_panel": panel,
            "files_generated": files,
            "statistics": self.stats.copy()
        }

    def _process_one_event(self, event_data, scenario, seq, total):
        """处理单个事件，返回结构化 step_data"""
        self.clock.advance(event_data.get('delay_ms', 0))
        raw = RawEvent(
            event_id=f"evt_{event_data['seq']}",
            timestamp_ns=self.clock.now_ns(),
            event_type=event_data['type'],
            pid=20000 + event_data['seq'], ppid=0,
            agent_id=event_data['agent'], agent_framework="test",
            executable=event_data.get('executable', ''),
            arguments=event_data.get('arguments', []),
            file_path=event_data.get('file_path', ''),
            file_op=event_data.get('file_op', ''),
            remote_addr=event_data.get('remote_addr', ''),
            remote_port=event_data.get('remote_port', 0),
        )
        norm = self.normalizer.normalize(raw)
        if 'normal' in event_data.get('agent', ''):
            self.baseline.collect(norm)
        match = self.engine.match(norm)
        context = self.normalizer.get_agent_context(event_data['agent'])
        self.scorer.set_baseline(self.baseline.get_baseline_dict())
        assessment = self.scorer.assess(norm, match, context)
        decision = self.decision_engine.decide(
            assessment, event_id=raw.event_id, agent_id=event_data['agent'])

        blocking_result = self.blocking_coord.execute(norm, decision)
        self.behavior_graph.add_event(
            norm, assessment=assessment, decision=decision,
            blocking_result=blocking_result,
            matched_rules=[r.rule_id for r in match.matched_rules])
        desc = self._build_desc(norm)
        self.audit_logger.log_event(
            norm, assessment=assessment, decision=decision,
            blocking_result=blocking_result,
            matched_rules=[r.rule_id for r in match.matched_rules],
            description=desc)

        # 更新统计
        self.stats['total'] += 1
        if assessment.overall_score > self.stats['max_score']:
            self.stats['max_score'] = assessment.overall_score
        if blocking_result.blocked:
            self.stats['block'] += 1
        elif decision.action == DecisionAction.ALERT:
            self.stats['alert'] += 1
        else:
            self.stats['allow'] += 1

        return {
            "type": "step", "seq": seq, "total": total,
            "event_type": norm.event_type,
            "raw_input": desc,
            "normalized_summary": norm.command_string or f"{norm.event_type}: {desc}",
            "rule_match": {
                "hit": match.has_match,
                "rules": [{"id": r.rule_id, "name": r.name, "action": r.action}
                          for r in match.matched_rules]
            },
            "risk_score": assessment.overall_score,
            "risk_level": assessment.risk_level.value,
            "risk_breakdown": {
                "rule": assessment.dimension_scores[0].score if len(assessment.dimension_scores) > 0 else 0,
                "baseline": assessment.dimension_scores[1].score if len(assessment.dimension_scores) > 1 else 0,
                "context": assessment.dimension_scores[2].score if len(assessment.dimension_scores) > 2 else 0,
                "sequence": assessment.dimension_scores[3].score if len(assessment.dimension_scores) > 3 else 0,
            },
            "decision_action": decision.action.value,
            "decision_tier": decision.tier.value,
            "disposal_summary": self._disposal_text(blocking_result, decision),
            "timestamp_ns": norm.timestamp_ns,
        }

    def _build_desc(self, norm):
        if norm.event_type == "exec":
            return norm.command_string or f"{norm.raw.executable} {' '.join(norm.raw.arguments or [])}"
        elif norm.event_type == "file_open":
            return f"{norm.raw.file_op} {norm.raw.file_path}"
        elif norm.event_type == "net_conn":
            return f"{norm.raw.remote_addr}:{norm.raw.remote_port}"
        return norm.event_type

    def _disposal_text(self, blocking_result, decision):
        if blocking_result.blocked:
            return "硬中断! 进程树终止" if blocking_result.tier == ActionTier.TIER3 else "阻止访问! 返回 EPERM"
        elif decision.action == DecisionAction.ALERT:
            return "告警! 记录审计日志"
        return "放行"

    def _build_analysis_panel(self, scenario):
        from analysis_panels import SA
        sid = scenario['id']
        prefix = ''
        for part in sid.split('-'):
            if part and part[0].isalpha() and part[0].lower() in 'nabme':
                prefix = part.lower()
                break
        data = SA.get(prefix, (scenario.get('description', ''), '', '', ''))
        return {
            "test_purpose": data[0], "risk_findings": data[1],
            "root_cause": data[2], "comprehensive_strategy": data[3],
            "max_risk_score": self.stats['max_score'],
            "matched_rules": self.stats['matched_rules'],
        }

    def _generate_reports(self, scenario):
        files = []
        report_path = self.report_exporter.export_scenario_report(
            scenario_id=scenario['id'], scenario_name=scenario['name'],
            audit_logger=self.audit_logger, behavior_graph=self.behavior_graph,
            scenario_description=scenario.get('description', ''),
            expected_result=scenario.get('expected_result', ''))
        files.append({"type": "md", "path": os.path.relpath(report_path, self.base_dir)})
        graph_path = self.run_mgr.graph_filepath(f'graph_{scenario["id"]}.json')
        self.behavior_graph.save_json(graph_path)
        files.append({"type": "json", "path": os.path.relpath(graph_path, self.base_dir)})
        if self.audit_logger.current_file:
            files.append({"type": "jsonl", "path": os.path.relpath(
                self.audit_logger.current_file, self.base_dir)})
        if self.stats['block'] > 0:
            evidence_path = self.blocking_coord.save_evidence(
                filepath=self.run_mgr.evidence_filepath(f'evidence_{scenario["id"]}.json'))
            files.append({"type": "json", "path": os.path.relpath(evidence_path, self.base_dir)})
        return files
```

### 5.5 服务器启动与关闭

```python
import webbrowser
from http.server import ThreadingHTTPServer, BaseHTTPRequestHandler

active_runs = {}      # run_id -> Queue
run_results = {}      # run_id -> result dict
run_status = {}       # run_id -> "running" / "completed" / "error"
run_all_progress = {"completed": 0, "total": 0, "current_scenario": "", "status": "idle"}

def main():
    host, port = "localhost", 8080
    try:
        server = ThreadingHTTPServer((host, port), ObserverHTTPHandler)
    except OSError as e:
        print(f"端口 {port} 被占用: {e}")
        return

    print(f"方寸观察者 Web 模式启动")
    print(f"   地址: http://{host}:{port}")
    webbrowser.open(f"http://{host}:{port}")

    try:
        server.serve_forever()
    except KeyboardInterrupt:
        print("\n正在关闭服务器...")
        for run_id, q in list(active_runs.items()):
            try:
                q.put_nowait({"type": "error", "message": "Server shutting down", "code": "server_shutdown"})
            except Exception:
                pass
        time.sleep(0.3)
        server.shutdown()
        print("服务器已关闭")
```

---

## 第六部分：`analysis_panels.py` 共享模块

### 6.1 设计目的

将 `demo.py` 中硬编码的 SA 字典提取为独立模块，`demo.py` 和 `app.py` 共同引用，确保 CLI 和 Web 模式展示一致的分析面板数据。

### 6.2 文件位置与结构

```
observer_sim/
├── analysis_panels.py    <-- 新增 (~100行)
├── demo.py               <-- 修改 1 行: from analysis_panels import SA
└── app.py                <-- 引用: from analysis_panels import SA
```

### 6.3 数据格式

```python
"""
analysis_panels.py -- 37 个场景的分析面板数据

每个场景: (目的, 风险发现, 归因分析, 处置策略)
key 为场景 ID 前缀 (如 "n01", "a01")
"""

SA = {
    'n01': ('验证标准开发流程不产生误报，建立基线',
            '无风险事件，全部ALLOW',
            '正常开发操作，无规则命中',
            '无需处置，正常放行并保存基线快照'),
    'a01': ('检测并阻断rm -rf /等高危破坏性命令',
            '命中R001(block-dangerous-commands)，rm -rf /被BLOCK',
            '命令模式rm -rf /直接匹配R001最高优先级规则，RuleMatchScore=1.0',
            'Tier2阻断当前exec事件，发送block_event反向指令'),
    # ... 共 37 个条目 (与 demo.py 中 SA 字典完全一致)
}
```

### 6.4 `demo.py` 改动

仅需 1 行修改：删除 `demo.py` 中原有的 SA 字典定义，替换为：

```python
from analysis_panels import SA
```

---

## 第七部分：文件清单与代码量估算

### 7.1 新增文件

| 文件 | 行数 | 说明 |
|------|:---:|------|
| `app.py` | ~450 | Web 服务器 + API + SSE + StreamScenarioRunner |
| `static/index.html` | ~550 | 单页面应用（HTML + CSS + JS） |
| `analysis_panels.py` | ~100 | 37 场景分析面板数据 |
| **新增总计** | **~1100** | |

### 7.2 现有文件改动

| 文件 | 改动量 | 说明 |
|------|:---:|------|
| `observer_core/` | **0 行** | 不改动 |
| `scenarios/` | **0 行** | 不改动 |
| `rules/` | **0 行** | 不改动 |
| `demo.py` | **~1 行** | SA 字典替换为 import |
| `main.py` | **0 行** | 不改动 |

### 7.3 新增目录

```
observer_sim/
├── static/              <-- 新增目录
│   └── index.html       <-- 前端单文件
```

---

## 第八部分：开发排期（3 天）

```
Day 1 (8h): 后端骨架 + 静态文件 + 基础 API
  ├─ [1h] app.py: ThreadingHTTPServer + ObserverHTTPHandler + 路由框架
  ├─ [1h] 静态文件服务 (/ -> static/index.html)
  ├─ [1h] analysis_panels.py: 提取 SA 字典 + demo.py 1行改动
  ├─ [2h] JSON API: categories, scenarios, reports/list, health, reports/view(含路径校验)
  ├─ [1h] index.html: HTML 骨架 + CSS 完整样式
  ├─ [1h] JS: 导航 + 视图切换 + 分类/场景列表渲染
  └─ [1h] 联调: 浏览器看到分类 -> 点击显示场景列表 -> 报告浏览

Day 2 (8h): SSE + 场景运行 + 前端交互
  ├─ [2h] StreamScenarioRunner 适配层 (核心: 复用初始化, yield step_data)
  ├─ [1.5h] SSE 端点: 单场景 stream + 全量 run-all + 进度查询
  ├─ [0.5h] 服务器关闭处理 (活跃 SSE 清理 + 线程 shutdown)
  ├─ [2h] JS: SSE 消费 + 日志渲染 + 分析面板 + 重连逻辑
  ├─ [1h] JS: 停止按钮 + logBuffer 恢复 + 视图状态管理
  └─ [1h] 联调: 点击运行 -> 逐步日志 -> 分析面板 -> 停止功能

Day 3 (8h): 完整功能 + 打磨
  ├─ [1.5h] 单元测试端点 (同步模式, subprocess.run + 结果解析)
  ├─ [1h] 报告查看 + Markdown 渲染 + 路径校验验证
  ├─ [1h] 删除记录 (三级: 全部/按分类/按场景)
  ├─ [1.5h] 全量运行: 进度条 + 统计面板 + 崩溃恢复
  ├─ [1h] CSS 打磨 + 响应式适配 + 通知组件
  ├─ [1h] 浏览器兼容测试 (Chrome/Edge/Firefox)
  └─ [0.5h] 文档 (README 更新, 使用说明)

注: 如果进度超预期，Day 3 的"全量运行崩溃恢复"可降级为"仅支持从头重跑"。
```

---

## 第九部分：边界条件与容错

| 场景 | 处理策略 |
|------|----------|
| 浏览器在场景运行中途关闭 | SSE 断开 -> BrokenPipeError -> 后台线程继续执行 -> Queue 最终 GC |
| 多 Tab 同时运行同一场景 | 每个 run 独立 run_id，互不干扰 |
| 场景运行超长（>30s 无新事件） | SSE 每 15s 发送 heartbeat |
| 请求不存在的 run_id | 返回 404 |
| 请求不存在的场景 ID | POST 返回 404 + error 信息 |
| 后台线程执行异常 | try/except -> Queue put error -> 前端显示错误 |
| 服务器端口被占用 | 启动时 OSError -> 提示用户 |
| SSE 连接中途断开 | onerror -> 先查 result 状态 -> 已完成则渲染，运行中则重建 SSE |
| 全量运行某场景失败 | 记录 error，继续下一个，不中断整体流程 |
| 服务器关闭时有活跃 SSE | 遍历 active_runs 发送 error -> 500ms -> shutdown |
| 路径遍历攻击 | os.path.realpath() + 前缀校验，返回 403 |
| 并发写入 active_runs | CPython dict 原子 + Queue 线程安全，无需加锁 |

---

## 第十部分：测试检查点（前端功能验收标准）

| 编号 | 测试项 | 通过标准 |
|:---:|--------|----------|
| T1 | 浏览器打开 localhost:8080 | 显示首页，侧边栏导航可见，5 个分类卡片正确渲染 |
| T2 | 点击"按分类浏览场景" | 显示 5 个分类卡片，场景数正确 (8/12/8/5/4) |
| T3 | 点击某分类的"浏览场景" | 显示该分类下所有场景列表 |
| T4 | 点击某场景的"运行" | 切换到运行视图，SSE 建立，逐步显示事件日志 |
| T5 | 场景运行结束 | 分析面板显示，4 个维度内容完整 |
| T6 | F12 Network EventStream | 看到逐步推送的 SSE 事件，heartbeat 每 15 秒 |
| T7 | 运行中切换视图再切回 | 日志从 logBuffer 恢复 |
| T8 | 运行中点击"停止" | SSE 断开，日志停止，显示通知 |
| T9 | 点击"全量运行" | 进度条显示，37 场景逐个运行，统计面板正确 |
| T10 | 全量运行中断恢复 | 进度查询返回正确进度 |
| T11 | 点击"运行单元测试" | Loading 动画，10-30 秒后结果面板，130 passed |
| T12 | 点击"查看报告" | 文件树显示，点击查看内容，Markdown 渲染 |
| T13 | 路径遍历攻击 | 构造 path=../../config.yaml，返回 403 |
| T14 | 点击"删除记录" | 三级删除确认，删除后消失，场景 ID 目录保留 |
| T15 | 响应式测试 | 缩小窗口，布局调整合理 |
| T16 | 多 Tab 并发 | 两个 Tab 运行不同场景，互不干扰 |
| T17 | Ctrl+C 关闭 | 无异常报错，端口释放 |
| T18 | 130 单元测试仍通过 | analysis_panels.py 提取后 demo.py 不受影响 |

---

## 第十一部分：与现有代码的接口对接清单

| 对接点 | 现有代码 | app.py 使用方式 |
|--------|---------|------------------|
| 事件归一化 | `EventNormalizer(clock, window_size)` | StreamScenarioRunner.__init__() 中初始化 |
| 规则匹配 | `RuleEngine.load_rules()` + `match()` | 同上 |
| 风险评分 | `RiskScorer.register_default_dimensions()` + `assess()` | 同上 |
| 基线检查 | `BaselineChecker` + `collect()` + `get_baseline_dict()` | 同上 |
| 研判决策 | `DecisionEngine.decide()` | 同上 |
| 阻断执行 | `BlockingCoordinator.execute()` | 同上 |
| 行为图谱 | `BehaviorGraph.add_event()` + `save_json()` | 同上 |
| 审计日志 | `AuditLogger.log_event()` + `start_scenario()` + `close()` | 同上 |
| 报告导出 | `ReportExporter.export_scenario_report()` | 同上 |
| 输出路径 | `RunOutputManager` + `infer_category()` | 同上 |
| 场景加载 | `yaml.safe_load()` 读取 scenarios/ | 同 demo.py |
| 分析面板 | `analysis_panels.SA` 字典 | 与 demo.py 共享数据源 |
| 单元测试 | `subprocess.run(["pytest", ...])` | 同 demo.py 逻辑 |
| 删除功能 | `shutil.rmtree()` + 目录扫描 | 同 demo.py 逻辑 |

---

## 第十二部分：一句话总结

> **在现有 CLI 系统基础上新增 `app.py`（~450行，`ThreadingHTTPServer` 标准库）和 `static/index.html`（~550行，原生三件套），通过 `StreamScenarioRunner` 适配层将现有 `observer_core/` 处理管道 yield 为结构化字典，经 SSE 实时推送到浏览器。核心逻辑零改动，CLI 与 Web 并存。`analysis_panels.py` 作为单一数据源供两端共享。`python app.py` 一键启动，浏览器自动打开，3 天完成全部开发。**