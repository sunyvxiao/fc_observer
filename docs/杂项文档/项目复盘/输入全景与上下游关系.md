graph TB
    subgraph 上游剧本层
        Y1["经典 YAML 场景<br/>37 个（事件级剧本）"]
        Y2["DeepAgent YAML 场景<br/>5 个（工具级剧本）"]
    end

    subgraph 行为产生层
        S1["SimulationCollector<br/>VirtualClock 播放"]
        S2["DeepAgentCollector<br/>simulation 播放"]
        S3["真实 pydantic-deep Agent<br/>Docker 内 + Hook 捕获"]
        S4["strace / eBPF 采集器<br/>观测真实 syscall"]
    end

    Y1 --> S1
    Y2 --> S2
    S3 -.Hook 工具调用.-> S4

    subgraph 事实层
        R["RawEvent 统一事件流<br/>（14 字段，event_type 必填）"]
    end

    S1 --> R
    S2 --> R
    S3 --> R
    S4 --> R

    subgraph 载体层
        J["JSONL 文件（录像带·一等公民）"]
        C["CSV 文件（兼容格式·降级）"]
        F["FIFO 管道（直播流·实时在线）"]
    end

    R -->|"EventRecorder/SessionRecorder 录制"| J
    R -->|"FifoWriter 写入"| F
    J -.->|"FileReplayCollector 回放"| R
    C -.->|"FileReplayCollector 回放"| R
    F -->|"monitor_daemon 实时消费"| R
    F -->|"旁路录制 --record"| J

    subgraph 消费层
        O["observer_core 全链路<br/>归一化→规则→评分→研判→阻断→审计"]
    end

    R --> O