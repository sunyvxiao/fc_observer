graph TB
    subgraph 生产环境
        AGENT["真实 Agent 进程<br/>零依赖 FifoWriter"]
        FIFO["FIFO 管道<br/>/tmp/observer_monitoring_pipe"]
        MON["monitor_daemon 守护进程"]
    end

    AGENT -->|JSON Lines 实时写入| FIFO
    FIFO -->|阻塞读取| MON
    MON -->|实时研判+阻断| OBS1["observer_core 全链路"]
    MON -->|"旁路录制 --record（解析前落盘）"| REC1["records/*.jsonl 录像带"]

    subgraph 回放环境
        REPLAY["FileReplayCollector<br/>加载 JSONL/CSV"]
        OBS2["observer_core 全链路<br/>（与实时链路同一套）"]
    end

    REC1 -->|"test.py --replay / ReplayEngine"| REPLAY
    REPLAY --> OBS2

    subgraph 模拟环境
        SCEN["YAML 场景（剧本）"]
        SIM["SimulationCollector（播放器）"]
        REC2["SessionRecorder<br/>records/&lt;timestamp&gt;/"]
    end

    SCEN --> SIM
    SIM -->|"RawEvent 流（分身）"| REC2
    SIM -->|RawEvent 流| OBS3["observer_core 全链路"]
    REC2 --> REPLAY

    OBS1 -.同构：回放结果等于实时结果.-> OBS2
    OBS3 -.同构.-> OBS2