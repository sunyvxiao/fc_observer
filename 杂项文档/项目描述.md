# 方寸观察者模拟学习系统 — 项目描述

> 让每一个 AI Agent 的行为都可控、可追溯、可信赖

---

## 一、项目背景

### AI Agent 时代，行为监控为何至关重要？

随着 AI 大模型的快速发展，越来越多的企业开始部署 AI Coding Agent 来辅助软件开发。这些 Agent 能够自主编写代码、执行命令、访问文件系统，甚至进行网络通信。

然而，**自主性越强，风险越大**：

- **误操作风险**：Agent 可能执行危险命令（如删除系统文件），造成不可逆的损失
- **数据泄露风险**：Agent 可能读取敏感信息（如密钥、配置文件）并通过网络外传
- **串谋风险**：多个 Agent 可能协同作案——一个负责收集数据，另一个负责传输
- **信任缺失**：企业无法判断 Agent 的行为是否合规，缺乏有效的监管手段

**方寸观察者模拟学习系统**正是为解决这些问题而生。它模拟了真实生产环境中 AI Agent 行为监控系统的核心能力，展示了如何在不侵入 Agent 运行的前提下，实现全方位的行为观测、风险评估和主动防护。

---

## 二、系统能解决什么问题？

### 核心痛点与解决方案

| 痛点 | 我们的解决方案 |
|------|-------------|
| Agent 做了什么？无法感知 | **全链路行为采集**：每一条命令、每一次文件访问、每一个网络连接都被完整记录 |
| 哪些行为是危险的？难以判断 | **智能风险评分**：四维评估模型自动量化风险等级，从"低风险"到"紧急"一目了然 |
| 发现危险行为如何阻止？ | **三级梯度阻断**：从温和告警到强制终止，递进式防护策略兼顾安全与业务连续性 |
| 事后如何追溯？如何定责？ | **全链路审计**：每个决策都有据可查，每步操作都有因果链，生成可读的分析报告 |
| 多个 Agent 串谋怎么办？ | **跨 Agent 关联检测**：自动识别不同 Agent 之间的协同行为，发现隐藏的串谋模式 |

---

## 三、产品功能亮点

### 1. 无感知的行为观测

系统像一位"隐形观察者"，在 Agent 完全无感知的情况下，实时采集其所有行为：
- 执行了什么命令？
- 访问了哪些文件？
- 连接了什么网络地址？

所有行为数据通过安全通道传输，不影响 Agent 的正常运行。

### 2. 智能四维风险评估

不是简单的"黑名单"匹配，而是通过四个维度综合评估每个行为的风险程度：

- **规则命中**（40%）：是否触发了已知的危险模式？（如 `rm -rf /`、向可疑 IP 发送数据）
- **基线偏离**（25%）：与 Agent 的历史正常行为相比，是否异常？
- **上下文风险**（20%）：近期行为序列是否可疑？（如"先读密钥文件，再连接外部服务器"）
- **序列异常**（15%）：行为模式是否偏离正常规律？

综合评分从 0 到 1，自动划分为低/中/高/紧急四个风险等级。

### 3. 三级梯度阻断

采用"递进式"防护策略，在安全性和业务连续性之间取得最佳平衡：

| 级别 | 名称 | 处理方式 | 适用场景 |
|:---:|------|---------|---------|
| 1 | 软报告 | 操作正常执行，系统记录告警 | 中等风险行为，需要关注但不必阻止 |
| 2 | 阻止访问 | 操作被阻止，Agent 收到"权限不足"反馈 | 高危操作，需要立即制止 |
| 3 | 硬中断 | Agent 进程被强制终止 | 极端危险行为，必须立即停止一切活动 |

**智能升级机制**：如果同一个 Agent 短时间内多次触发低级告警，系统会自动升级处置力度——从"提醒"升级到"阻止"，再到"强制终止"。

### 4. 全链路审计与报告

每个行为从采集到处置的完整过程都会被记录：
- **审计日志**：结构化的 JSON 格式，便于程序化分析和合规审查
- **风险分析报告**：自动生成 Markdown 格式的可读报告，包含风险分布、阻断明细、规则统计
- **行为图谱**：以图结构展示 Agent 的行为序列和因果关系，支持跨 Agent 关联分析

### 5. 跨 Agent 串谋检测

当多个 Agent 在同一时间窗口内出现可疑的协同行为时，系统会自动建立关联关系，识别潜在的串谋模式。例如：
- Agent-A 读取了敏感文件
- 1 秒内，Agent-B 开始向外部服务器发送数据
- 系统自动检测并标记这种跨 Agent 的可疑关联

### 6. 37 场景全面测试体系

系统内置了 **37 个精心设计的测试场景**，覆盖 5 大分类：

| 分类 | 数量 | 覆盖内容 |
|------|:---:|--------|
| 正常行为 | 8 | 日常开发、构建部署、定时任务等，验证不误判 |
| 异常行为 | 12 | 高危命令、数据窃取、反弹Shell、挖矿等，验证检测和阻断 |
| 边界场景 | 8 | 模糊地带操作，验证评分精度和分级合理性 |
| 多Agent协作 | 5 | 合法协作、隐性串谋、环境投毒、权限提升链 |
| 极端场景 | 4 | 高速事件流、超长参数、管道故障、超时降级 |

每个场景运行后自动生成 **4 维度分析面板**：测试目的、风险发现、归因分析、处置策略——让安全团队快速理解每个场景的意义和系统表现。

### 7. 交互式演示控制台

系统提供全新交互式演示界面，启动后展示清晰的主菜单：
- **单元测试模式**：一键运行 130 个自动化测试，输出结构化结果摘要
- **全量模拟模式**：静默运行全部 37 个场景，输出全局数据统计面板（事件总数、放行/告警/阻断分布、5 分类统计）
- **分类浏览模式**：按分类逐步浏览，支持运行整个分类或指定单个场景

---

## 四、系统工作流程

系统的工作流程就像一条精密的"流水线"，每个行为从发生到处置，要经过七个站点的完整处理：

```
事件采集 → 归一化 → 规则匹配 → 风险评分 → 研判决策 → 阻断执行 → 审计输出
```

### 第一步：事件采集

系统通过底层探针（模拟 eBPF 技术）实时捕获 Agent 的系统调用行为。探针分为三类：
- **进程探针**：捕获命令执行（如 git clone、python run）
- **文件探针**：捕获文件操作（如读取、写入、删除）
- **网络探针**：捕获网络连接（如 HTTP 请求、DNS 查询）

### 第二步：归一化处理

原始事件被转换为统一的标准格式，同时补充上下文信息：
- 这个事件属于哪个 Agent？
- 这个 Agent 之前做了什么？
- 这个进程的父进程是谁？

### 第三步：规则匹配

将事件与预设的安全策略规则进行匹配。系统内置了 15 条安全规则，覆盖三大类：
- **命令执行规则**：识别危险命令（如删除、格式化、提权操作）
- **文件操作规则**：识别敏感文件访问（如密钥文件、配置文件）
- **网络规则**：识别可疑网络行为（如外联未知 IP、大流量传输）

### 第四步：风险评分

通过四维评估模型计算综合风险分数（0~1），并划分风险等级：
- **LOW**（< 0.3）：正常行为，无需干预
- **MEDIUM**（0.3 ~ 0.6）：需要关注，可能触发告警
- **HIGH**（> 0.6）：高风险，需要阻止或终止

### 第五步：研判决策

基于风险等级和规则匹配结果，通过研判矩阵做出最终决策：
- **ALLOW**（放行）：行为安全，允许继续执行
- **ALERT**（告警）：行为存在风险，记录并通知
- **BLOCK**（阻断）：行为危险，必须阻止

### 第六步：阻断执行

根据决策等级执行对应的处置动作：
- Tier1 软报告：放行 + 记录告警
- Tier2 阻止访问：阻止操作 + 返回权限错误
- Tier3 硬中断：终止 Agent 进程 + 紧急保存证据

### 第七步：审计输出

所有处理结果被完整记录：
- 写入 JSON 审计日志（每步操作都有据可查）
- 更新行为图谱（记录行为间的因果关系）
- 生成风险分析报告（供安全团队审阅）

---

## 五、三级梯度阻断的业务意义

传统的安防系统往往是"非黑即白"——要么全部放行，要么全部阻止。但实际业务中，过于激进的阻断会影响正常业务，过于宽松则可能放过真正的威胁。

**三级梯度阻断**提供了一种更灵活、更智能的防护策略：

### Tier1 软报告 —— "我知道，我记录"

- **业务影响**：零。Agent 的操作正常完成
- **系统动作**：记录审计日志，更新风险画像
- **业务价值**：不干扰正常业务，同时积累行为数据用于后续分析

### Tier2 阻止访问 —— "这个不行"

- **业务影响**：当前操作被阻止，但 Agent 仍可继续其他操作
- **系统动作**：返回权限错误，记录阻断原因
- **业务价值**：精准阻止危险操作，不影响 Agent 的整体工作流程

### Tier3 硬中断 —— "立即停止一切活动"

- **业务影响**：Agent 被强制终止
- **系统动作**：终止进程树，紧急保存所有证据，生成事故报告
- **业务价值**：在极端情况下（如数据正在被外传），以最快的速度止损

### 自动升级机制

系统会追踪每个 Agent 的违规历史。如果同一个 Agent 在短时间内反复触发告警，处置力度会自动升级：

```
第 1-2 次告警 → Tier1 软报告（观察期）
第 3 次告警   → 自动升级到 Tier2（阻止访问）
被阻止 2 次后 → 自动升级到 Tier3（强制终止）
```

这种"先礼后兵"的策略，既给了 Agent 改正的机会，也确保了持续违规时能够果断处置。

---

## 六、演示效果展示

系统提供全新交互式演示控制台，运行后可以直观看到完整的行为监控流程。

### 运行方式

```bash
python demo.py
```

启动后展示主菜单，选择对应选项即可。也支持命令行直接指定场景：

```bash
python demo.py --scenario a01          # 运行单个场景
python demo.py --category anomalous    # 运行整个异常分类
python demo.py --auto                  # 全场景自动播放
```

### 逐事件可视化流水线

演示过程中，终端会逐步展示每个事件的处理流水线：

```
── 事件 [2/3] ────────────────────────────────────────
>> [输入] exec: /bin/rm -rf / --no-preserve-root
   ├─ [N] [归一化] 命令字符串: /bin/rm -rf / --no-preserve-root...
   ├─ [R] [规则匹配] 命中规则: R001(block) [!]
   ├─ [S] [风险评分] 0.40 (MEDIUM)
   ├─ [D] [研判决策] ALERT (TIER1) [!]
   ├─ [!] [处置结果] 阻止访问! 返回 EPERM
   │   └─ [反向管道] block_event -> event=evt_2
   │   └─ [升级!] TIER1 -> TIER2 (违规累计升级)
```

### 场景分析面板（4 维度）

每个场景运行结束后，自动生成结构化分析面板：

```
┌──────────────────────────────────────────────────────────┐
│ 场景分析面板                                              │
├──────────────────────────────────────────────────────────┤
│ ① 测试目的                                                │
│    检测并阻断 rm -rf / 等高危破坏性命令                     │
├──────────────────────────────────────────────────────────┤
│ ② 风险发现                                                │
│    命中 R001(block-dangerous-commands)，rm -rf / 被 BLOCK  │
│    命中规则: R001(score=0.40)                              │
│    最高风险评分: 0.40                                      │
├──────────────────────────────────────────────────────────┤
│ ③ 风险成因（归因分析）                                     │
│    命令模式 rm -rf / 直接匹配 R001 最高优先级规则           │
├──────────────────────────────────────────────────────────┤
│ ④ 综合处置策略                                             │
│    Tier2 阻断当前 exec 事件，发送 block_event 反向指令...   │
└──────────────────────────────────────────────────────────┘
```

### 全局统计面板

全量模拟模式下，运行结束后输出全局统计：

```
┌──────────────────────────────────────────────────────────┐
│ 全量模拟测试 — 全局数据统计                                │
├──────────────────────────────────────────────────────────┤
│ 总体统计                                                  │
│ 总事件数:    274                                          │
│ 总放行(ALLOW):  239                                       │
│ 总告警(ALERT):  8                                         │
│ 总阻断(BLOCK):   27                                       │
├──────────────────────────────────────────────────────────┤
│ 分类统计                                                  │
│ Normal   正常       放行: 56  告警:  1  阻断:  0          │
│ Anomalous 异常      放行: 22  告警:  2  阻断: 27          │
│ Boundary 边界       放行: 20  告警:  2  阻断:  0          │
│ Multi-Agent 多Agent 放行: 31  告警:  2  阻断:  0          │
│ Extreme  极端       放行:110  告警:  1  阻断:  0          │
└──────────────────────────────────────────────────────────┘
```

### 生成的文件

运行结束后，`output/reports/` 目录下按 **分类 → 场景ID → 时间戳** 自动归档：
- **审计日志**：JSON 行格式，每行一条完整处理记录
- **风险分析报告**：Markdown 格式，包含风险分布、阻断明细、规则统计
- **行为图谱**：JSON 格式，记录所有事件节点和因果关系
- **阻断证据**：JSON 格式，记录所有阻断事件的完整信息

时间戳目录确保每次运行不会覆盖历史结果，同秒运行自动追加编号。

---

## 七、技术架构概览

系统采用**分层设计**，各层职责清晰，通过标准化接口通信：

```
┌─────────────────────────────────────────────┐
│            C++ 探针模拟层                     │
│   模拟 eBPF 内核探针，采集系统调用事件        │
│   通过命名管道与 Python 层双向通信             │
└──────────────────┬──────────────────────────┘
                   │ 双向命名管道
                   │ (事件上报 + 指令下发)
                   ▼
┌─────────────────────────────────────────────┐
│          Python 业务逻辑层                    │
│                                              │
│  监测层 → 评判层 → 阻断层 → 审计层           │
│                                              │
│  虚拟时钟：驱动全系统时间判定                  │
│  自优化接口：为后续迭代预留扩展点              │
└─────────────────────────────────────────────┘
```

**核心设计特点**：

- **全链路透明**：每个处理阶段的输入输出均可查看、可追溯
- **模块可替换**：评分算法、阻断策略、输出格式均可独立替换
- **零外部依赖**：Python 仅需一个第三方库（pyyaml），C++ 仅需标准库
- **数据本地化**：所有数据存储在本地文件，不依赖网络，不上传云端

---

## 八、未来扩展能力

系统在设计之初就为后续迭代预留了扩展空间：

### 自优化能力（已预留接口）

- **行为模式挖掘**：自动从历史审计数据中发现新的异常模式
- **策略自动生成**：基于挖掘到的模式，自动生成新的安全规则
- **规则效果验证**：新规则在应用到生产环境前，先进行回溯测试验证

### 可扩展方向

- **更精细的评分算法**：替换基础评分算法为机器学习模型，提升判定精度
- **更多探针类型**：扩展 DNS 解析监控、内存操作监控等
- **实时监控面板**：接入 Web 前端，提供实时行为监控大屏
- **多租户支持**：支持同时监控多个 Agent 团队，独立管理策略和权限

---

## 九、项目信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 方寸观察者模拟学习系统 |
| 项目性质 | 教学原型 / 概念验证 |
| 目标平台 | Windows |
| 技术栈 | Python 3.10+ / C++17 |
| 测试场景 | 37 个场景，5 大分类（正常/异常/边界/多Agent/极端） |
| 自动化测试 | 130 个测试用例，全部通过 |
| 演示方式 | 交互式主菜单 + 分类浏览 + 命令行模式 |
| 输出管理 | 分类+时间戳归档，防覆盖，结构化报告 |

---

*方寸观察者 —— 于方寸之间，洞察秋毫。*
