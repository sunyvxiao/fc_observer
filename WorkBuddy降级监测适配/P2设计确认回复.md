# P2 设计确认回复

- 回复对象：《监测与拦截解耦双路径_开发落地计划.md》第 417-440 行「P2 设计输出」
- 回复时间：2026-09-05 20:51 (GMT+8)
- 回复人：WorkBuddy（agent_id=workbuddy，session_id=wb-20260905-163855）

## 结论：A —— 按此设计进入开发

设计完整、可落地，同意按「P2 设计输出」的四子任务（P2-1~P2-4）进入开发，无需结构性修改。

## 设计评审意见（简要）

- **P2-1**：`from_hook_entry` 补全 hook 条目 → RawEvent 专用映射，post 审计经 `/api/hook-report` 入流，路径正确；「留痕不变（双 jsonl 第 2 轨）」守住了解耦原则，同意。
- **P2-2**：三类不一致定义清晰（unreported_hook_deny / hook_blindspot_change / report_snapshot_conflict），四源输入 + 损坏行容错，复用 `_iter_jsonl`，合理。
- **P2-3**：前端改动成本高，先「报告页 + 数据 API」的降级路径务实，前端渲染列为可选增量正确，同意。
- **P2-4**：复用 P1-3 `snapshot_checker_unavailable` 语义，各维度独立 unavailable、不中断核对，正确。

## 附：宿主实测环境状态（2026-09-05 20:51 实测）

| 组件 | 状态 | 实测依据 |
|---|---|---|
| WorkBuddy（本宿主） | ✅ 在线 | 正在响应本次请求 |
| observer MCP 通道 | ✅ 在线 | ToolSearch 命中 report_tool_call / report_session |
| mcp_report daemon (8765) | ✅ 在线 | HTTP 可达（根路径 404 属正常，无根路由） |
| hook_gate notify 端点 (127.0.0.1:9090/hook) | ⚠️ 当前未监听 | HTTP:000 连接失败（与 18:42 观测到的 notify 超时一致） |

## 不阻塞开发的注意事项（非修改点，开发期一并处理）

1. **notify 目标端点口径**：P2-1 ② 的 notify 必须指向 `/api/hook-report`（8765 端口，`hook_ingest.path`），不要误复用 `hook_gate.notify_url`（9090，当前未监听）。`_notify_payload` 只复用「模式/代码结构」，目标 URL 需替换。
2. **快照路径归一化口径**：P2-2 的 report_snapshot_conflict 依赖快照 4663 文件路径与申报 target 路径的关联，大小写 / 斜杠方向 / 相对-绝对需在 P2-2 明确统一归一化口径，否则易误报。
3. **RawEvent 字段数勘误**：P2 任务表（第 410 行）写「14 字段」，现状调研（第 423 行）写「15 字段（models/event.py）」，以代码 15 字段为准，建议统一文档表述。

（完）
